# Developer verification tools.
#
# Keep runtime dependencies in requirements.txt. This file is for local/CI
# checks that do not need to be installed by an app user.
pytest
ruff
bandit
pip-audit
# Static type checking (QUAL-002). The stub packages give mypy real types for
# the requests/yaml call sites instead of treating those libraries as Any.
mypy
types-requests
types-PyYAML
# pandas typing (QUAL-006). pandas-stubs tracks the pinned pandas minor
# series; types-pytz is its declared dependency and is pinned alongside it.
pandas-stubs
types-pytz
# Coverage measurement for the CI gate (QUAL-003).
pytest-cov
# Local commit-time checks mirroring CI's non-rewriting hooks.
pre-commit

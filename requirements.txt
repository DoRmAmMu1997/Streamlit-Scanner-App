# Web UI.
streamlit

# Google SSO (OIDC) login for the Streamlit auth gate. Streamlit's st.login()
# imports Authlib at runtime to run the Google redirect and raises
# StreamlitAuthError without it. Authlib is not a Streamlit transitive
# dependency, so the app must declare it explicitly.
authlib

# Data handling and calculations.
pandas
numpy
pyarrow

# Persistence layer schema (SCAN-001). SQLAlchemy 2.0 defines the
# scan_runs / scan_results ORM models in backend/storage/models.py. The
# engine, sessions, and Alembic migrations that USE this schema are SCAN-002.
SQLAlchemy
# Alembic is SQLAlchemy's migration tool. It creates/updates the physical
# database tables from versioned migration files instead of ad-hoc create_all().
alembic
# Psycopg 3 is the PostgreSQL driver used by postgresql+psycopg:// URLs.
# The binary extra provides a self-contained wheel for normal deployments.
psycopg[binary]

# Dhan API and environment loading.
requests
python-dotenv
dhanhq

# Scheduled jobs: YAML daily-scan schedule (JOB-002). backend/jobs/daily_scan_config
# imports `yaml` directly to parse config/daily_scans.yaml, so PyYAML is declared
# explicitly rather than relied on as someone else's transitive dependency.
PyYAML

# Fundamental analysis: screener.in scraper + Claude Agent SDK agent.
# `claude-agent-sdk` runs the Check Fundamentals agent on your Claude
# subscription (bundled Claude CLI login — no API key, no Node required).
# `pdfplumber` extracts text from downloaded concall transcript PDFs.
beautifulsoup4
lxml
pdfplumber
claude-agent-sdk

# Test runner.
pytest

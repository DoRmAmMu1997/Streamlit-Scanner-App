# Direct dependency pins used by local verification and GitHub Actions.
#
# Beginner note: `requirements.txt` says which packages the app needs.
# `constraints.txt` says which exact direct versions we have verified together.
# Install with `pip install -r requirements.txt -c constraints.txt` so routine
# setup is repeatable, then refresh these pins deliberately during dependency
# maintenance PRs.

# Runtime application dependencies.
# streamlit 1.58 is the floor for pandas 3.x: 1.55 caps pandas<3, so the two
# pins move together. 1.58 also makes starlette/uvicorn required rather than
# optional extras, and drops tornado.
streamlit==1.64.0
authlib==1.8.0
pandas==3.0.6
numpy==2.5.3
pyarrow==25.0.1
requests==2.34.2
# pdf_transport.py uses urllib3's inspected 2.7.0 pool API directly; this
# declares the already-tested transitive version without upgrading it.
urllib3==2.8.0
python-dotenv==1.2.3
dhanhq==2.2.0
PyYAML==6.0.3
beautifulsoup4==4.15.0
lxml==6.1.3
pdfplumber==0.11.10
claude-agent-sdk==0.2.157
SQLAlchemy==2.0.54
# Keep Alembic pinned with SQLAlchemy so local migrations and CI use the same
# migration behavior every time.
alembic==1.20.0
# `psycopg[binary]` installs both distributions; pin each one so CI and
# deployments use the same PostgreSQL driver implementation.
psycopg==3.3.6
psycopg-binary==3.3.6

# Developer and CI verification tools.
pytest==9.1.1
ruff==0.16.3
bandit==1.9.4
pip-audit==2.10.1
mypy==2.3.1
types-requests==2.33.0.20260712
types-PyYAML==6.0.12.20260518
# pandas-stubs tracks the pinned pandas 3.0.5 series (QUAL-006); types-pytz is
# its declared dependency.
pandas-stubs==3.0.5.260730
types-pytz==2026.3.1.20260727
pytest-cov==7.1.0
pre-commit==4.6.2
